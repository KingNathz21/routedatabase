(() => { const button=document.querySelector('#themeButton'); if(!button)return; const apply=dark=>{document.body.classList.toggle('dark',dark);button.textContent=dark?'Light mode':'Dark mode'}; apply(localStorage.getItem('rfl-theme')==='dark'); button.addEventListener('click',()=>{const dark=!document.body.classList.contains('dark');localStorage.setItem('rfl-theme',dark?'dark':'light');apply(dark)}); })();